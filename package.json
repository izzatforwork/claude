{
  "name": "sop-screen-snippet-recorder",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension: record a click-through workflow and export it as a step-by-step SOP .docx document.",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "docx": "^9.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
